{% if authors is not empty %}
    <h3> We have {{authors |length }} main authors : </h3>
    <ul>
        {% for author in authors %}
            <li>
            <img src="{{ asset('images/' ~ author.picture) }}" alt="{{ author.nom }}" width="50px" height="50px">
            {{ author.nom| upper }} ({{ author.getNbBooks() }} books)
            <a href="{{ path('app_author_details', {id: author.id}) }}">Details</a>
            </li>
            
        {% endfor %}
    </ul>
{% else %}
    <p>No authors available.</p>
{% endif %}